# =============================================================================
# Lambda Backend – Optimized Python Dependencies for AWS Lambda
# =============================================================================

# Web framework
fastapi>=0.115.0

# Lambda adapter for FastAPI
mangum>=0.18.0

# Settings / validation
pydantic>=2.0
pydantic-settings>=2.0

# AWS SDK (Bedrock) - boto3 is available in Lambda runtime but specify for local testing
boto3>=1.34.0

# HTTP client for Supabase REST queries
requests>=2.31.0

# PDF parsing for CV uploads
PyPDF2>=3.0.0

# Required by Starlette/FastAPI to parse multipart FormData (file uploads)
python-multipart>=0.0.9

# Environment variables
python-dotenv>=1.0.0

# MLflow tracing & experiment tracking (genai extra includes token/cost parsing)
mlflow[genai]>=3.1.0

# PostgreSQL driver – required for direct-DB fallback when MLflow REST server
# is unreachable (MLFLOW_TRACKING_URI_FALLBACK=postgresql://...)
psycopg2-binary>=2.9.0
